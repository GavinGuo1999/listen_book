# 2026-06-20 Progress

## 今天完成

- 新增正式浏览器 E2E smoke。
  - `frontend/playwright.config.ts`
  - `frontend/e2e/books.spec.ts`
  - `frontend/package.json`
  - `frontend/package-lock.json`
  - `frontend/tsconfig.json`
  - `.gitignore`
  - 覆盖首页加载、上传 TXT、等待解析、阅读进度恢复、删除临时测试书。
  - E2E 不触发真实 TTS，避免依赖外部网络。

- 扩展音频接口和失败路径测试。
  - `backend/tests/test_audio_api.py`
  - 使用 fake TTS provider，避免测试依赖 `edge-tts` 网络访问。
  - 覆盖：
    - 生成音频资产并下载文件
    - 同一句音频缓存复用
    - sentence 不存在返回 404
    - provider 失败返回 502 并将 asset 标记为 `failed`
    - failed asset 可重试恢复为 `ready`
    - prefetch 创建 pending asset
    - status 查询 pending 状态
    - 文件端点拒绝未 ready、文件不存在、路径越界和 asset 不存在

- 完成 EPUB 解析最小版。
  - `backend/app/services/epub.py`
  - `backend/app/workers/parse_books.py`
  - `backend/tests/test_books_api.py`
  - 不新增第三方依赖，使用标准库读取 EPUB zip。
  - 读取 `META-INF/container.xml` 找 OPF。
  - 按 OPF manifest/spine 顺序读取 XHTML/HTML。
  - 提取正文文本，按章节/段落/句子入库。
  - 无效 EPUB 会标记书籍为 `failed`。

- 完成登录/多用户进度隔离最小闭环。
  - `backend/app/api/deps.py`
  - `backend/app/api/routes/auth.py`
  - `backend/app/schemas/auth.py`
  - `backend/app/services/auth.py`
  - `backend/app/api/routes/books.py`
  - `backend/app/services/progress.py`
  - `backend/tests/test_auth_api.py`
  - `frontend/src/App.tsx`
  - `frontend/src/api.ts`
  - `frontend/src/styles.css`
  - `frontend/src/types.ts`
  - 新增：
    - `POST /api/auth/register`
    - `POST /api/auth/login`
    - `GET /api/auth/me`
  - 前端新增账号卡、登录/注册表单、退出登录。
  - API 自动携带 localStorage token。
  - 无 token 时仍兼容默认 `local` 用户。
  - 登录后阅读进度按用户隔离。

- 更新文档。
  - `README.md`
  - `docs/runbook.md`
  - 记录 TXT/EPUB 支持、E2E、auth 和测试覆盖范围。

## 今天提交

- `7f6dee0 test: add browser e2e and audio api coverage`
- `7454f13 feat: parse epub uploads`
- `ce3047c feat: add auth and user progress isolation`

## 今天验证

- 用户实机运行 E2E 通过：

```text
2 passed (6.8s)
```

- 后端测试通过：

```powershell
.venv\Scripts\python.exe -m pytest backend\tests -q
# 15 passed
```

- 后端 lint 通过：

```powershell
.venv\Scripts\ruff.exe check --no-cache backend\app backend\tests scripts\smoke_api.py
# All checks passed
```

- 前端构建通过：

```powershell
cd D:\listen_book\frontend
npm run build
```

- diff 格式检查通过：

```powershell
git diff --check
```

## 当前注意事项

- 本地 `master` 比 `origin/master` 多 3 个功能提交；今天收工文档也需要提交后再统一推送。
- 后端和前端代码都有变更，明天浏览器测试前需要重启服务：

```bat
scripts\stop-dev.bat
scripts\start-dev.bat
```

- 登录/多用户目前是最小闭环：
  - 已有注册、登录、token、当前用户、退出登录、阅读进度隔离。
  - 尚未做完整书库归属/权限隔离。
  - 未登录本地模式仍保留。
- 浏览器 E2E 目前仍覆盖未登录本地模式；登录链路 E2E 尚未补。
- EPUB 解析是最小实现，对常见 spine XHTML 可用；复杂 EPUB 的目录、脚注、图片、CSS 排版清理还没系统优化。
- PDF 解析仍未实现。

## 明天建议测试顺序

1. 启动服务：

```bat
scripts\stop-dev.bat
scripts\start-dev.bat
```

2. 检查服务：

- `http://127.0.0.1:8000/api/health`
- `http://127.0.0.1:5173/`

3. 跑自动化：

```powershell
cd D:\listen_book
.venv\Scripts\python.exe -m pytest backend\tests -q

cd D:\listen_book\frontend
npm run test:e2e
```

4. 浏览器手测：

- 未登录本地模式：上传 TXT、阅读、切换句子、刷新恢复进度。
- 注册新用户并登录。
- 登录后上传 TXT 和 EPUB，确认解析到 `ready`。
- 登录用户 A 保存进度后退出；未登录或用户 B 不应恢复到 A 的进度。
- 退出登录后应回到本地模式。

## 下一个优先任务

1. 补登录/注册浏览器 E2E。
2. 决定书库模型：公共书库、用户私有书库，还是混合。
3. 按决定实现书籍归属/权限隔离。
4. 继续 PDF 解析或增强 EPUB 解析质量。
5. 改善小说朗读体验。
